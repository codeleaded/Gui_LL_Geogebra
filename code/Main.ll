from io import *
from sys import *
from math import *

function pow(a,b)
    return math::pow(a,b)
end

function main(x)
    --out = x * x
    --return 1.0 / (1.0 + math::pow(2.71828,-x))
    --return x * x
    return pow(2.0,x)
end